<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit File Name</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .card {
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 12px rgba(0, 0, 0, 0.2);
        }
    </style>
</head>
<body class="bg-gray-900 text-white flex items-center justify-center min-h-screen">

    <div class="card bg-gray-800 p-8 rounded-lg w-full max-w-md">
        <a class="text-blue-400 text-xl inline-block mb-6 hover:underline" href="/">Go back to home</a>
        <h1 class="text-2xl font-semibold mb-6 text-center">Edit File Name</h1>
        <form action="/edit" method="post">
            <input class="block w-full mb-4 rounded-md px-4 py-2 bg-gray-700 text-white" type="text" placeholder="Previous Name" name="previous" value="<%= filename %>">
            <input class="block w-full mb-4 rounded-md px-4 py-2 bg-gray-700 text-white" type="text" placeholder="New Name" name="new">
            <input class="w-full bg-yellow-600 px-4 py-2 rounded-md text-white hover:bg-yellow-700 cursor-pointer" type="submit" value="Update Name">
        </form>
    </div>

</body>
</html>
